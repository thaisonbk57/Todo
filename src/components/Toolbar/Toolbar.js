import React from "react";

//////////////////////////

import "./Toolbar.scss";

///////////////////////////

import Logo from "./../UI/Logo/Logo";

const Toolbar = props => {
  return (
    <div className="Toolbar">
      <Logo />
    </div>
  );
};

export default Toolbar;
